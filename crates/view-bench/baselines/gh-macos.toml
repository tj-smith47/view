schema = 1
engine_pin = "v0.12.4"
machine_class = "gh-macos"

[withdrawn.first_paint.heavy]
marker_ratio_p50 = "taken on a harness pty that never answered nvim's DSR; re-seat from the artifact of the first gate run after the responder fix"
marker_ratio_p99 = "taken on a harness pty that never answered nvim's DSR; re-seat from the artifact of the first gate run after the responder fix"

[withdrawn.first_paint.minimal]
marker_ratio_p50 = "taken on a harness pty that never answered nvim's DSR; re-seat from the artifact of the first gate run after the responder fix"
marker_ratio_p99 = "taken on a harness pty that never answered nvim's DSR; re-seat from the artifact of the first gate run after the responder fix"

[withdrawn.first_paint.user]
marker_ratio_p50 = "taken on a harness pty that never answered nvim's DSR; re-seat from the artifact of the first gate run after the responder fix"
marker_ratio_p99 = "taken on a harness pty that never answered nvim's DSR; re-seat from the artifact of the first gate run after the responder fix"

[withdrawn.startup.minimal]
server_delta_ms = "taken on a harness pty that never answered nvim's DSR; re-seat from the artifact of the first gate run after the responder fix"
settled_ratio_p50 = "taken on a harness pty that never answered nvim's DSR; re-seat from the artifact of the first gate run after the responder fix"

[withdrawn.echo.user]
paired_delta_p99_ms = "seated 2026-09-06 as the real-config leg the felt bound is stated on; no quiet-host run has recorded it yet, so the gate reports it rather than attesting to a bar nobody took"
ratio_p50 = "seated 2026-09-06 as the real-config leg the felt bound is stated on; no quiet-host run has recorded it yet, so the gate reports it rather than attesting to a bar nobody took"
ratio_p99 = "seated 2026-09-06 as the real-config leg the felt bound is stated on; no quiet-host run has recorded it yet, so the gate reports it rather than attesting to a bar nobody took"
view_p99_ms = "seated 2026-09-06 as the real-config leg the felt bound is stated on; no quiet-host run has recorded it yet, so the gate reports it rather than attesting to a bar nobody took"

[withdrawn.echo_speculated.user]
speculated_paint_p99_ms = "seated 2026-09-06 as the real-config leg the felt bound is stated on; no quiet-host run has recorded it yet, so the gate reports it rather than attesting to a bar nobody took"
speculated_ratio_p50 = "seated 2026-09-06 as the real-config leg the felt bound is stated on; no quiet-host run has recorded it yet, so the gate reports it rather than attesting to a bar nobody took"

[withdrawn.scroll.user]
ratio_p50 = "seated 2026-09-06 as the real-config leg the felt bound is stated on; no quiet-host run has recorded it yet, so the gate reports it rather than attesting to a bar nobody took"
ratio_p99 = "seated 2026-09-06 as the real-config leg the felt bound is stated on; no quiet-host run has recorded it yet, so the gate reports it rather than attesting to a bar nobody took"
staleness_p99_ms = "seated 2026-09-06 as the real-config leg the felt bound is stated on; no quiet-host run has recorded it yet, so the gate reports it rather than attesting to a bar nobody took"

[withdrawn.flood.user]
cadence_p99_ms = "seated 2026-09-06 as the real-config leg the felt bound is stated on; no quiet-host run has recorded it yet, so the gate reports it rather than attesting to a bar nobody took"
cadence_p99_ratio = "seated 2026-09-06 as the real-config leg the felt bound is stated on; no quiet-host run has recorded it yet, so the gate reports it rather than attesting to a bar nobody took"
pace_ratio = "seated 2026-09-06 as the real-config leg the felt bound is stated on; no quiet-host run has recorded it yet, so the gate reports it rather than attesting to a bar nobody took"

[withdrawn.startup.user]
first_frame_cold_ms = "seated 2026-09-06 as the real-config leg the felt bound is stated on; no quiet-host run has recorded it yet, so the gate reports it rather than attesting to a bar nobody took"
first_frame_ratio_p99 = "seated 2026-09-06 as the real-config leg the felt bound is stated on; no quiet-host run has recorded it yet, so the gate reports it rather than attesting to a bar nobody took"
server_delta_ms = "seated 2026-09-06 as the real-config leg the felt bound is stated on; no quiet-host run has recorded it yet, so the gate reports it rather than attesting to a bar nobody took"
settled_ratio_p50 = "seated 2026-09-06 as the real-config leg the felt bound is stated on; no quiet-host run has recorded it yet, so the gate reports it rather than attesting to a bar nobody took"

[echo.heavy]
paired_delta_p99_ms = 4.27
ratio_p50 = 1.148999015425008
ratio_p99 = 1.0146224631287029
view_p99_ms = 8.049

[echo.minimal]
paired_delta_p99_ms = 3.4139999999999997
ratio_p50 = 1.10267471958585
ratio_p99 = 1.221824686940966
view_p99_ms = 5.464

[echo_control.heavy]
control_delta_p99_ms = 3.683
control_p99_ms = 7.366
control_ratio_p50 = 0.9811781108400138
control_ratio_p99 = 1.0685834082060497

[echo_control.minimal]
control_delta_p99_ms = 1.5939999999999999
control_p99_ms = 3.345
control_ratio_p50 = 1.0019854401058903
control_ratio_p99 = 1.0143098479578654

[echo_speculated.minimal]
speculated_paint_p99_ms = 1.38
speculated_ratio_p50 = 0.3044368600682594

[first_paint.heavy]
marker_cold_ms = 148.065584
shell_visible_cold_ms = 33.354875

[first_paint.minimal]
marker_cold_ms = 71.101125
shell_visible_cold_ms = 33.911583

[first_paint.user]
marker_cold_ms = 169.09875
shell_visible_cold_ms = 40.38275

[flood.minimal]
cadence_p99_ms = 13.50475
cadence_p99_ratio = 0.9748820330938244
pace_ratio = 1.0310112926401895

[input_path.minimal]
key_to_rpc_p99_us = 191.0

[memory.minimal]
phys_footprint_mb = 3.4696044921875

[output_path.minimal]
p99_ms = 0.295

[picker.minimal]
first_page_p50_ms = 3.0648329999999997
first_page_p99_ms = 29.880584000000002
match_paint_p50_ms = 4.80825
match_paint_p99_ms = 30.797167

[remote_memory.minimal]
local_phys_footprint_mb = 4.8604736328125
phys_footprint_mb = 4.7589111328125
remote_local_ratio = 0.9807117562850038

[scroll.heavy]
ratio_p50 = 2.180922826499431
ratio_p99 = 1.3815426623133977
staleness_p99_ms = 4.831791

[scroll.minimal]
ratio_p50 = 1.7748667724060885
ratio_p99 = 1.2774489839209198
staleness_p99_ms = 4.929125

[startup.minimal]

[supervision.minimal]
restart_rehydrate_p99_ms = 65.65383299999999
wedge_detect_p99_ms = 11576.124875

[echo.user]

[echo_speculated.user]

[scroll.user]

[flood.user]

[startup.user]
